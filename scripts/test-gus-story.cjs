const assert=require('node:assert/strict'),fs=require('node:fs'),E=require('../public/3d/story-state.js'),b=JSON.parse(fs.readFileSync('public/3d/story/briar-foundation.json'));
let s,n=0;function reset(){s=E.create();s.flags['thomas.ready']=true}function act(type,x={}){const r=E.transact(s,b,{id:'gus-'+ ++n,type,...x});s=r.state;return r.changed}function pick(choice){return act('choose',{line:s.conversation.node,choice})}function open(){assert(act('open',{npc:'gus'}))}
reset();open();assert(pick('help'));assert(pick('accept'));assert(!pick('deliver'));act('close');const checkpoint=E.restore(s);assert(act('world',{key:'briar.gus.tools'}));assert(!act('world',{key:'briar.gus.tools'}));open();assert(pick('deliver'));assert.equal(s.items.gus_tools,0);assert.equal(s.rewards['briar.gus.BR-02'].kind,'rift_shard');assert(s.flags['gus.shortcut']);assert(!pick('deliver'));assert.equal(E.restore(checkpoint).rewards['briar.gus.BR-02'],undefined);assert(pick('care'));act('close');open();assert.equal(E.view(s,b).lineId,'briar.gus.done');
reset();open();assert(pick('mock'));assert(!pick('repair'));assert(pick('sorry'));act('close');assert(act('world',{key:'briar.gus.sign'}));open();assert(pick('repair'));assert(pick('help'));assert(pick('accept'));assert.equal(s.quests['briar.gus.tools'],'active');
reset();open();assert(pick('mock'));assert(pick('cruel'));act('close');assert(!act('world',{key:'briar.gus.sign'}));assert(!act('world',{key:'briar.gus.tools'}));assert(act('open',{npc:'mara'}),'main quest remains available');act('close');open();assert.equal(E.view(s,b).choices.length,0);assert.equal(E.restore(s).quests['briar.gus.tools'],'closed');reset();open();assert.equal(E.view(s,b).choices.length,3);
console.log('Gus passed: delivery gating, once-only reward, shortcut, resume, rollback, apology/repair, final refusal, main-path independence, fresh attempt.');
