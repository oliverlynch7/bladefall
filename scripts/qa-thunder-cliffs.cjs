async page=>{
const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:4331/3d/?mute=1');await page.waitForFunction(()=>window.__BF3&&HERO3D?.ready);await page.evaluate(async()=>{const b=__BF3;await b.briarReady;b.loadMode('rl');b.meta.run=null;b.meta.bank=null;b.meta.introSeen=true;b.meta.classId='warrior';b.meta.riftShards=[];b.meta.autoAttack=false;b.meta.petActive=null;b.openHub();b.enterZone(5);b.nextArea();b.meta.tutOff=true;b.meta.camMode='far';b.G.p.invuln=999;for(const e of b.G.enemies)e.stunT=999;});await page.waitForFunction(()=>!BF_LOADING.active&&__world3d().counts.name==='Thunder Cliffs');
const result=await page.evaluate(()=>{const b=__BF3,G=b.G,p=G.p,checks=[],walks=[],ok=(n,v)=>{if(!v)throw Error(n);checks.push(n)},act=key=>{const o=G.storyObjects.find(o=>o.key==='tc.'+key);Object.assign(p,{x:o.x,z:o.z,y:o.y,vy:0});b.briarRequest('world',{key:o.key});},talk=(id,choices)=>{const o=G.storyNpcs.find(o=>o.id===id);Object.assign(p,{x:o.x,z:o.z,y:o.y});b.briarRequest('open',{npc:id});for(const choice of choices)b.briarRequest('choose',{line:G.storyState.conversation.node,choice});b.briarRequest('close');};
ok('no disguised boss troops',!G.boss&&!G.enemies.some(e=>e.boss));for(const e of G.enemies){e.dead=true;e.hp=0;}
act('lift');ok('brake required before release',!G.thunder.lift);act('brake');act('lift');ok('lift restored',G.thunder.lift);BFThunderCliffs.ride(G,true);for(let i=0;i<150;i++)b.update(.016);ok('lift reaches upper station',Math.abs(p.y-340)<3);
act('approach');ok('inspect chain before opening approach',!G.portal);act('shackle');act('approach');ok('main open without optional NPC',!!G.portal&&!G.storyState.flags['tc.dash.met']);talk('dash',['claim','help']);act('cave.help');ok('lower brace required',!G.storyState.flags['tc.cave.open']);act('brace');act('cave.help');act('sail');ok('cave physically opens',!G.walls.some(w=>w.thunderTag==='caveGate'));act('tide.3');ok('wrong sequence stays closed',!G.storyState.flags['tc.tide.open']);[1,2,3].forEach(i=>act('tide.'+i));ok('tidal wall opens',!G.walls.some(w=>w.thunderTag==='tideGate'));
for(const o of b.worldRiftShards()){Object.assign(p,{x:o.x,z:o.z,y:o.y});b.takeWorldRiftShard(o.id);}ok('three personal shards provisional',G.pendingRiftShards.found.length===3&&!b.meta.riftShards.length);
for(const [name,path]of Object.entries({...BFThunderCliffs.paths,race:G.thunder.race})){const a=path[0];Object.assign(p,{x:a[0],z:a[1],y:a[2],vy:0,vx:0,vz:0,onGround:true});for(const q of path){for(let i=0;i<1600;i++){const dx=q[0]-p.x,dz=q[1]-p.z,d=Math.hypot(dx,dz);if(d<16)break;b.input.jx=dx/d;b.input.jz=dz/d;b.update(.016);}b.input.jx=b.input.jz=0;walks.push({name,to:q,at:[Math.round(p.x),Math.round(p.z),Math.round(p.y)],ok:Math.hypot(p.x-q[0],p.z-q[1])<28&&Math.abs(p.y-q[2])<65});}}
return {checks,waypoints:walks.length,failed:walks.filter(w=>!w.ok),counts:__world3d().counts};});if(result.failed.length||errors.length)throw Error(JSON.stringify({failed:result.failed,errors}));return {...result,errors};}
