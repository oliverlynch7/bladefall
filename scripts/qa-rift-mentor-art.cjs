async page=>{
 const errors=[],results=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:4331/3d/?mute=1');await page.waitForFunction(()=>window.__BF3&&window.HERO3D?.ready);await page.setViewportSize({width:1440,height:900});await page.evaluate(async()=>{const b=__BF3;await b.briarReady;b.loadMode('rl');b.meta.run=null;b.meta.bank=null;b.meta.classId='warrior';b.meta.introSeen=true;b.meta.tutOff=true;b.openHub();});
 for(const cid of ['berserker','ninja','reaper','chronomancer','pirate','paladin','necromancer']){
  await page.evaluate(cid=>{const b=__BF3;b.openHub();b.startTrial(cid,Math.max(0,b.ZONES.findIndex(z=>z.side?.trial===cid)));for(const e of b.G.enemies)e.stunT=999;b.G.p.invuln=999;b.meta.camMode='classic';},cid);await page.waitForFunction(cid=>!BF_LOADING.active&&window.__npcArtStats?.ids?.includes('mentor_'+cid),cid);
  const r=await page.evaluate(()=>{const b=__BF3,g=b.G,n=g.storyNpcs[0],p=g.p;function walk(x,z){for(let i=0;i<140;i++){const dx=x-p.x,dz=z-p.z,d=Math.hypot(dx,dz);if(d<8)break;b.input.jx=dx/d;b.input.jz=dz/d;b.update(.016)}b.input.jx=b.input.jz=0;return Math.hypot(x-p.x,z-p.z)<20&&Math.abs(p.y-n.y)<60&&b.briarInteract()?.label==='Talk to '+n.name}const start=walk(n.x,n.z+60);g.trialKills=b.TRIALS[g.trial].kills;b.trialMentorProved();Object.assign(p,{x:n.x+75,z:n.z+75,y:n.y});const end=walk(n.x,n.z+60);return {id:n.id,start,end,pos:[p.x,p.y,p.z],target:[n.x,n.y,n.z+60],face:__npcArtStats.faces[n.id]};});results.push(r);
  if(cid==='berserker'||cid==='paladin'){await page.evaluate(()=>{const b=__BF3,n=b.G.storyNpcs[0];b.G.enemies=[];b.briarRequest('open',{npc:n.id});document.getElementById('hubReveal')?.click();});await page.waitForTimeout(650);await page.screenshot({path:'output/playwright/mentor-'+cid+'.png'});await page.keyboard.press('Escape');}
 }
 await page.evaluate(()=>__BF3.openHub());if(errors.length||results.some(r=>!r.start||!r.end))throw Error(JSON.stringify({errors,results}));return {results,errors};
}