// Local preview controls only, loaded behind ?visualqa=1.
if(new URLSearchParams(location.search).has('visualqa')){
const panel=document.createElement('details');panel.open=true;panel.style.cssText='position:fixed;right:8px;top:8px;z-index:99999;background:#111e;color:#eee;border:1px solid #b98b4e;padding:8px;max-width:360px;font:11px monospace';
panel.innerHTML='<summary>Outskirts preview controls</summary><div id="qa-buttons"></div><select id="qa-room"></select><button id="qa-look">Visit district</button><button id="qa-report">Report</button><button id="qa-overhead">Camera</button><button id="qa-quality">Low quality</button><button id="qa-hub">Waystation</button><pre id="qa-data" style="max-height:230px;overflow:auto;white-space:pre-wrap"></pre>';document.body.append(panel);
const b=()=>window.__BF3;let fps=0,frameCount=0,t0=performance.now();function tick(t){frameCount++;if(t-t0>1000){fps=Math.round(frameCount*1000/(t-t0));frameCount=0;t0=t}requestAnimationFrame(tick)}requestAnimationFrame(tick);
function digest(){const g=b().G;const counts={};for(const d of g.deco)counts[d.kind||d.theme||'box']=(counts[d.kind||d.theme||'box']||0)+1;let hash=2166136261;const str=JSON.stringify([g.segments,g.obstacles,g.walls,g.doors,g.chests.map(c=>[c.x,c.y,c.z]),g.portal&&[g.portal.x,g.portal.z]]);for(let i=0;i<str.length;i++){hash^=str.charCodeAt(i);hash=Math.imul(hash,16777619)}return {fps,quality:b().meta.quality,version:b().snap().version,area:g.area,side:g.side,stage:g.stageIndex,player:[g.p.x,g.p.y,g.p.z],collisionHash:(hash>>>0).toString(16),segments:g.segments.length,obstacles:g.obstacles.length,rooms:g.rooms.map(r=>({name:r.name,x:r.x,z:r.z,w:r.w,d:r.d})),deco:counts,world:window.__world3d?.(),render:window.__BF_RENDER_STATS,hero:window.HERO3D&&{ready:HERO3D.ready,err:HERO3D.err},hp:g.p.hp,enemies:g.enemies.length,questIds:Object.keys(g.qs||{})}}
function report(){document.querySelector('#qa-data').textContent=JSON.stringify(digest(),null,2)}
function visit(area,side=false){const api=b();api.meta.classId='warrior';api.enterZone(0);api.G.runSeed=42042;api.G.area=area;api.G.side=side;api.loadArea();api.G.p.invuln=9999;document.querySelector('#qa-room').innerHTML=api.G.rooms.map((r,i)=>`<option value="${i}">${r.name||'District '+i}</option>`).join('');report()}
for(const [name,area,side] of [['Outskirts',0,false],['Black Woods',1,false],['Brute',-1,false],['Thornwood',1,true]]){const bt=document.createElement('button');bt.textContent=name;bt.onclick=()=>visit(area,side);document.querySelector('#qa-buttons').append(bt)}
for(const [name,area,side] of [['Hollow Pass',0,false],['Dry Wash',1,false],['Marksman',-1,false],['Sunken Wash',1,true]]){const bt=document.createElement('button');bt.textContent=name;bt.onclick=()=>{const api=b();api.loadMode('rl');api.meta.classId='warrior';api.enterZone(1);api.G.runSeed=42042;api.G.area=area;api.G.side=side;api.loadArea();api.G.p.invuln=9999;document.querySelector('#qa-room').innerHTML=api.G.rooms.map((r,i)=>`<option value="${i}">${r.name||'District '+i}</option>`).join('');report();};document.querySelector('#qa-buttons').append(bt)}
const exportButton=document.createElement('button');exportButton.textContent='Export art for Blender';exportButton.onclick=()=>{document.querySelector('#qa-data').textContent=JSON.stringify(window.__world3dArtSnapshot?.());};panel.append(exportButton);
for(const [name,area,side] of [['Ruined Keep',0,false],['Undercroft',1,false],['The Fallen',-1,false],['Oubliette',1,true]]){const bt=document.createElement('button');bt.textContent=name;bt.onclick=()=>{const api=b();api.loadMode('rl');api.meta.classId='warrior';api.enterZone(2);api.G.runSeed=42042;api.G.area=area;api.G.side=side;api.loadArea();api.G.p.invuln=9999;document.querySelector('#qa-room').innerHTML=api.G.rooms.map((r,i)=>`<option value="${i}">${r.name||'District '+i}</option>`).join('');report();};document.querySelector('#qa-buttons').append(bt)}
const layoutButton=document.createElement('button');layoutButton.textContent='Export level layout';layoutButton.onclick=()=>{const g=b().G;document.querySelector('#qa-data').textContent=JSON.stringify({area:g.area,side:g.side,segments:g.segments,obstacles:g.obstacles,walls:g.walls,deco:g.deco,rooms:g.rooms.map(r=>({x:r.x,z:r.z,y:r.y,w:r.w,d:r.d,name:r.name}))});};panel.append(layoutButton);
document.querySelector('#qa-look').onclick=()=>{const api=b(),r=api.G.rooms[+document.querySelector('#qa-room').value];api.look(r.x,r.z+(r.d||400)*.25,Math.PI,.16);const g=api.G;g.p.y=api.highestSurfaceAt(g.p.x,g.p.z,14)+2;g.p.vy=0;g.cam.x=g.p.x;g.cam.z=g.p.z;g.cam.y=g.p.y;g.camYaw=Math.PI;g.camPitch=.16;report()};
document.querySelector('#qa-report').onclick=report;
document.querySelector('#qa-hub').onclick=()=>{const api=b();api.loadMode('rl');if(!Object.keys(api.meta.classUnlocked||{}).length){api.startTrial('warrior');api.skipTrial();}else api.enterHub();if(api.G)report();};
document.querySelector('#qa-quality').onclick=()=>{b().meta.quality=b().meta.quality==='low'?'high':'low';window.dispatchEvent(new Event('resize'));report();};
document.querySelector('#qa-overhead').onclick=()=>{b().meta.camMode=b().meta.camMode==='far'?'shoulder':'far';report()};
const hubControls=document.createElement('div');hubControls.innerHTML='<button id="qa-hub-check">Check hub paths</button><select id="qa-station"></select><button id="qa-approach">Approach station</button><button id="qa-use">Use station</button>';panel.append(hubControls);
function hubAudit(){
  const g=b().G;if(!g.hubArt)return {error:'Enter the new Waystation first'};
  const bounds=g.bounds,step=20,radius=14;
  const blocked=(x,z)=>(g.walls||[]).some(o=>Math.abs(x-o.x)<o.w/2+radius&&Math.abs(z-o.z)<o.d/2+radius);
  const key=(x,z)=>Math.round(x/step)+','+Math.round(z/step),queue=[[g.startPos.x,g.startPos.z]],seen=new Set([key(...queue[0])]);
  for(let i=0;i<queue.length;i++)for(const [dx,dz] of [[step,0],[-step,0],[0,step],[0,-step]]){const x=queue[i][0]+dx,z=queue[i][1]+dz,k=key(x,z);
    if(x<bounds.minX+radius||x>bounds.maxX-radius||z<bounds.minZ+radius||z>bounds.maxZ-radius||seen.has(k)||blocked(x,z))continue;
    seen.add(k);queue.push([x,z]);
  }
  return {version:b().snap().version,reachableFloorCells:seen.size,world:window.__world3d?.(),render:window.__BF_RENDER_STATS,
    approaches:g.hubArt.approaches.map(a=>({...a,clear:!blocked(a.x,a.z),withinInteractionRadius:Math.hypot(a.x-a.tx,a.z-a.tz)<(a.id==='waystone'?72:a.id==='tinkerer'?70:a.id.startsWith('gate-')||a.id.startsWith('side-')?96:110),reachable:seen.has(key(a.x,a.z))})),
    services:g.hubNpcs.map(n=>({id:n.id,x:n.x,z:n.z})),gates:g.gates.map(g=>({zi:g.zi,side:g.side,x:g.x,z:g.z,open:g.open}))};
}
document.querySelector('#qa-hub-check').onclick=()=>{const g=b().G;document.querySelector('#qa-station').innerHTML=(g.hubArt?.approaches||[]).map((a,i)=>`<option value="${i}">${a.id}</option>`).join('');document.querySelector('#qa-data').textContent=JSON.stringify(hubAudit(),null,2)};
document.querySelector('#qa-approach').onclick=()=>{const api=b(),g=api.G,a=g.hubArt.approaches[+document.querySelector('#qa-station').value];const yaw=Math.atan2(a.tx-a.x,a.tz-a.z);api.look(a.x,a.z,yaw,.16);g.cam.x=g.p.x;g.cam.z=g.p.z;g.cam.y=g.p.y;g.camYaw=yaw;g.camPitch=.16;document.querySelector('#qa-data').textContent=JSON.stringify({approach:a,position:{x:g.p.x,y:g.p.y,z:g.p.z},interaction:g.interact?.label,world:window.__world3d?.(),render:window.__BF_RENDER_STATS},null,2)};
document.querySelector('#qa-use').onclick=()=>{b().G.interact?.act?.();};
}
