async page=>{return page.evaluate(async()=>{const T=await import('./three.module.js'),{buildWorld,syncWorld,WORLD3D}=await import('./world3d.js?v=2031'),scene=new T.Scene(),segments=[{x:0,z:0,w:400,d:400},{x:190,z:0,w:400,d:100},{x:0,z:0,w:50,d:50}];const world={zone:'qa',theme:'stone',segments,deco:[],obstacles:[],walls:[],rooms:[],p:{x:0,y:0,z:0}};const original=window.__BF_WORLD;window.__BF_WORLD=()=>world;try{for(let i=0;i<100;i++){if(syncWorld(scene))break;await new Promise(r=>setTimeout(r,100));}}finally{window.__BF_WORLD=original;}const counts=buildWorld(scene,world);if(!counts.floorTiles)throw Error('No floor models loaded');let instances=0;scene.traverse(n=>{if(n.isInstancedMesh&&n.renderOrder===-1)instances+=n.count;});if(instances!==counts.floorTiles)throw Error('Incorrect floor count');return {floorTiles:counts.floorTiles,instances,drawCalls:counts.drawCalls};});}

