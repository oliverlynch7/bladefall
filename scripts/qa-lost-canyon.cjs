async page=>{
const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.reload();await page.waitForFunction(()=>window.__BF3&&HERO3D?.ready);await page.evaluate(async()=>{const b=__BF3;await b.briarReady;b.loadMode('rl');b.meta.run=null;b.meta.bank=null;b.meta.introSeen=true;b.meta.classId='warrior';b.meta.riftShards=[];b.openHub();b.enterZone(1);b.nextArea();b.meta.tutOff=true;for(const e of b.G.enemies)e.stunT=999;b.G.p.invuln=999;});await page.waitForFunction(()=>!BF_LOADING.active);await page.waitForTimeout(350);
const result=await page.evaluate(()=>{const b=__BF3,G=b.G,p=G.p,checks=[],walks=[];const ok=(n,v)=>{if(!v)throw Error(n);checks.push(n)};function act(key){const o=G.storyObjects.find(o=>o.key===key);Object.assign(p,{x:o.x,z:o.z,y:o.y});b.briarRequest('world',{key});}function talk(n,c){const o=G.storyNpcs.find(o=>o.id===n);Object.assign(p,{x:o.x,z:o.z,y:o.y});b.briarRequest('open',{npc:n});b.briarRequest('choose',{line:G.storyState.conversation.node,choice:c});b.briarRequest('close');}function walk(q){for(let i=0;i<800;i++){const dx=q[0]-p.x,dz=q[1]-p.z,d=Math.hypot(dx,dz);if(d<16)break;b.input.jx=dx/d;b.input.jz=dz/d;if(i%28===0)b.input.jumpEdge=true;b.update(.016);}b.input.jx=b.input.jz=0;walks.push({to:q,at:[Math.round(p.x),Math.round(p.z),Math.round(p.y)],ok:Math.hypot(p.x-q[0],p.z-q[1])<25&&p.y>=q[2]-48});}
ok('front gate initially solid',G.obstacles.some(o=>o.lcBlock==='gate'));act('lc.orders');talk('ward','orders');ok('written orders open physical gate',G.storyState.flags['lc.entry']&&!G.obstacles.some(o=>o.lcBlock==='gate'));talk('ruth','help');act('lc.cage.south');ok('live cage guards prevent release',!G.storyState.flags['lc.south']);act('lc.alarm');for(const e of G.enemies){e.hp=0;e.dead=true;}act('lc.cage.south');act('lc.cage.north');act('lc.cage.extra');ok('quiet rescue does not call wave',!G.storyState.flags['lc.alarm.called']);act('lc.escape');ok('main escape opens',G.portal&&G.canyon.shortcut);talk('ruth','reward');ok('optional shared shard granted',G.pendingRiftShards.found.includes('HP-03'));act('lc.wagon');ok('wagon collider removed',!G.obstacles.some(o=>o.lcBlock==='wagon'));act('lc.rail.north');act('lc.rail.east');ok('wrong rail sequence stays closed',!G.storyState.flags['lc.rail.open']);for(const d of ['north','west','south','east'])act('lc.rail.'+d);Object.assign(p,{x:-2280,z:-3890,y:0});b.takeWorldRiftShard('HP-04');ok('personal rail shard collected',G.pendingRiftShards.found.includes('HP-04'));
for(const [name,path]of Object.entries(G.canyon.walks)){const q=path[0];Object.assign(p,{x:q[0],z:q[1],y:q[2],vy:0});for(const q of path)walk(q);}
for(let i=0;i<3000;i++)b.update(.016);ok('all six prisoners reach shelter',G.canyonPrisoners.every(n=>!n.walking&&n.z<-3900));return {checks,waypoints:walks.length,failed:walks.filter(x=>!x.ok),prisoners:G.canyonPrisoners.map(n=>[n.id,n.x,n.y,n.z]),counts:__world3d().counts};});if(errors.length||result.failed.length)throw Error(JSON.stringify({errors,failed:result.failed}));return {...result,errors};
}
