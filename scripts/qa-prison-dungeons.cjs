async page=>{
const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:4331/3d/?mute=1');await page.waitForFunction(()=>window.__BF3&&HERO3D?.ready);await page.evaluate(async()=>{const b=__BF3;await b.briarReady;b.loadMode('rl');b.meta.run=null;b.meta.bank=null;b.meta.introSeen=true;b.meta.classId='warrior';b.meta.riftShards=[];b.openHub();b.enterZone(2);b.nextArea();b.meta.tutOff=true;b.meta.camMode='far';b.G.p.invuln=999;for(const e of b.G.enemies)e.stunT=999;});await page.waitForFunction(()=>!BF_LOADING.active&&__world3d().counts.keepArt);await page.waitForTimeout(350);
const r=await page.evaluate(()=>{const b=__BF3,G=b.G,p=G.p,checks=[],walks=[],ok=(n,v)=>{if(!v)throw Error(n);checks.push(n)},act=k=>{const o=G.storyObjects.find(o=>o.key===k);Object.assign(p,{x:o.x,z:o.z,y:o.y,vy:0});b.briarRequest('world',{key:k});},tick=n=>{for(let i=0;i<n;i++)b.update(.016);},clear=()=>{for(const e of G.enemies){e.dead=true;e.hp=0;}};
const talk=(n,cs)=>{const o=G.storyNpcs.find(o=>o.id===n);Object.assign(p,{x:o.x,z:o.z,y:o.y});b.briarRequest('open',{npc:n});for(const c of cs)b.briarRequest('choose',{line:G.storyState.conversation.node,choice:c});b.briarRequest('close');};
act('kd.hidden');ok('service door needs discovered roster',!G.storyState.flags['kd.hidden']);act('kd.roster');act('kd.marks');act('kd.hidden');act('kd.free');ok('living guards block captive release',!G.storyState.flags['kd.free']);clear();act('kd.free');act('kd.papers');ok('main clues work without NPC dialogue',G.storyState.flags['kd.papers']&&!G.storyState.flags['kd.walter.met']);act('kd.lift.1');ok('wrong load does not start lift',!G.storyState.flags['kd.lift.open']);act('kd.lift.1');act('kd.lift.0');act('kd.lift.2');ok('one plus three restores lift',G.storyState.flags['kd.lift.open']);tick(500);Object.assign(p,{x:0,z:-2500,y:180,vy:0,onGround:true});tick(280);const mid={player:p.y,lift:G.movers[0].h};ok('player rides rising lift',Math.abs(mid.player-mid.lift)<3&&mid.player>250);talk('walter',['help']);const t=G.prison.rescueT;const n=G.storyNpcs[0];Object.assign(p,{x:n.x,z:n.z,y:n.y});b.briarRequest('open',{npc:'walter'});tick(100);ok('dialogue freezes lift and rescue',G.prison.rescueT===t);b.briarRequest('close');tick(1200);ok('prisoners reach shelter and open final approach',G.storyState.flags['kd.transfer']&&G.portal&&G.keepPrisoners.every(n=>n.y===360&&!n.walking));
talk('sly',['ask','open']);act('kd.return');talk('sly',['returned']);ok('Sly restitution grants shared RK04 once',G.storyClaims['kd.sly.RK-04']&&G.pendingRiftShards.found.includes('RK-04'));act('kd.deep.1');ok('lower bank locked before safety latch',!G.storyState.items['kd.deep']);act('kd.brace.2');act('kd.latch');act('kd.deep.1');act('kd.deep.2');ok('two-stage lower load opens shard chamber',G.storyState.flags['kd.deep.open']);act('kd.cache');ok('dry store grants rare armor',G.storyClaims['kd.drain.armor']);for(const s of b.worldRiftShards()){Object.assign(p,{x:s.x,z:s.z,y:s.y});b.takeWorldRiftShard(s.id);}ok('three second-half shards stay provisional',G.pendingRiftShards.found.length===3&&!b.meta.riftShards.includes('RK-03'));
for(const [name,path]of Object.entries(G.prison.walks)){const first=path[0];Object.assign(p,{x:first[0],z:first[1],y:first[2],vy:0,onGround:true});for(const q of path){for(let i=0;i<550;i++){const dx=q[0]-p.x,dz=q[1]-p.z,d=Math.hypot(dx,dz);if(d<16)break;b.input.jx=dx/d;b.input.jz=dz/d;b.update(.016);}b.input.jx=b.input.jz=0;walks.push({name,to:q,at:[Math.round(p.x),Math.round(p.z),Math.round(p.y)],ok:Math.hypot(p.x-q[0],p.z-q[1])<26&&Math.abs(p.y-q[2])<55});}}
Object.assign(p,{x:2100,z:-3800,y:0,vy:0,onGround:true});tick(90);ok('deep water returns to drain entrance without death',Math.hypot(p.x-1500,p.z+3350)<30&&p.hp>0);return {checks,waypoints:walks.length,failed:walks.filter(x=>!x.ok),counts:__world3d().counts};});if(r.failed.length||errors.length)throw Error(JSON.stringify({failed:r.failed,errors}));return {...r,errors};
}
