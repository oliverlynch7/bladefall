async page=>{
const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:4331/3d/?mute=1');await page.waitForFunction(()=>window.__BF3&&HERO3D?.ready);await page.evaluate(async()=>{const b=__BF3;await b.briarReady;b.loadMode('rl');b.meta.run=null;b.meta.bank=null;b.meta.introSeen=true;b.meta.classId='warrior';b.meta.autoAttack=false;b.meta.petActive=null;b.meta.riftShards=['FF-01','FF-02','FF-03','FF-04','FF-05'];b.openHub();b.enterZone(3);b.nextArea();b.nextArea();b.meta.tutOff=true;b.meta.camMode='far';b.G.p.invuln=999;});await page.waitForFunction(()=>!BF_LOADING.active&&__world3d().counts.frostArt);await page.waitForTimeout(400);
const result=await page.evaluate(()=>{const b=__BF3,G=b.G,p=G.p,checks=[],ok=(n,v)=>{if(!v)throw Error(n);checks.push(n)},tick=n=>{for(let i=0;i<n;i++)b.update(.016);};ok('exactly three officers without adds',G.enemies.length===3&&G.enemies.every(e=>e.frostOfficer));for(const e of G.enemies)e.stunT=999;const failed=[];let points=0;
for(const [name,path]of Object.entries(BFFrostOfficers.paths)){Object.assign(p,{x:path[0][0],z:path[0][1],y:path[0][2],vx:0,vz:0,vy:0,onGround:true});for(let k=1;k<path.length;k++){const from=path[k-1],to=path[k],n=Math.ceil(Math.hypot(to[0]-from[0],to[1]-from[1])/70);for(let j=1;j<=n;j++){const x=from[0]+(to[0]-from[0])*j/n,z=from[1]+(to[1]-from[1])*j/n;for(let i=0;i<180;i++){const dx=x-p.x,dz=z-p.z,d=Math.hypot(dx,dz);if(d<13)break;b.input.jx=dx/d;b.input.jz=dz/d;b.update(.016);}points++;if(Math.hypot(p.x-x,p.z-z)>30||p.y< -5)failed.push({name,x,z,at:[p.x,p.z,p.y]});}}}b.input.jx=b.input.jz=0;ok('all ramp routes traverse',failed.length===0);
Object.assign(p,{x:0,z:100,y:0,invuln:999});for(const e of G.enemies){e.stunT=0;e.hp=e.maxHp;}let maxBusy=0,roles=new Set();for(let i=0;i<9000;i++){b.update(.016);maxBusy=Math.max(maxBusy,G.enemies.filter(BFFrostOfficers.busy).length);for(const e of G.enemies)if(e.offState==='strike')roles.add(e.frostOfficer);}ok('one major attack at a time',maxBusy===1);ok('all three officers can reach and attack',roles.size===3);ok('no legacy crystals or reinforcements',G.enemies.length===3&&!G.enemies.some(e=>e.shielded));
for(const e of G.enemies)e.stunT=999;const spear=G.enemies.find(e=>e.frostOfficer==='spear');Object.assign(spear,{stunT:0,offState:'strike',offClock:.3,offAttack:.3,offSeq:900,offX:0,offZ:0,offY:0,offDX:0,offDZ:1,offRange:400,offTravel:200});Object.assign(p,{x:0,z:100,y:0,hp:10000,invuln:0,dodgeTimer:0});const hp=p.hp;b.updateBoss(spear,0,p,0,1,100,0);ok('active spear causes actual damage',p.hp<hp);const after=p.hp;p.invuln=0;b.updateBoss(spear,0,p,0,1,100,0);ok('same attack only hits once',p.hp===after);spear.offSeq++;p.dodgeTimer=.3;b.updateBoss(spear,0,p,0,1,100,0);ok('dodge prevents damage',p.hp===after);p.dodgeTimer=0;p.invuln=999;
const initial=b.meta.bossKills||0;G.enemies[0].hp=0;b.killEnemy(G.enemies[0]);ok('caster death cannot end fight',!G.portal&&(b.meta.bossKills||0)===initial);G.enemies[1].hp=0;b.killEnemy(G.enemies[1]);ok('two deaths cannot open exit',!G.portal);spear.hp=0;b.killEnemy(spear);ok('last officer opens exit and one boss credit',!!G.portal&&(b.meta.bossKills||0)===initial+1&&!!G.storyState.notes['frost.ember']);ok('five banked shards retained',b.meta.riftShards.length===5);return {checks,points,failed,roles:[...roles],counts:__world3d().counts};});if(errors.length)throw Error(JSON.stringify(errors));return {...result,errors};
}
