async page=>{
const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:4331/3d/?mute=1');await page.waitForFunction(()=>window.__BF3&&HERO3D?.ready);await page.evaluate(async()=>{const b=__BF3;await b.briarReady;b.loadMode('rl');b.meta.run=null;b.meta.bank=null;b.meta.introSeen=true;b.meta.classId='warrior';b.meta.riftShards=[];b.openHub();b.enterZone(2);b.meta.tutOff=true;b.meta.camMode='far';b.G.p.invuln=999;for(const e of b.G.enemies)e.stunT=999;});await page.waitForFunction(()=>!BF_LOADING.active&&__world3d().counts.keepArt);await page.waitForTimeout(350);
const result=await page.evaluate(()=>{const b=__BF3,G=b.G,p=G.p,checks=[],walks=[];const ok=(n,v)=>{if(!v)throw Error(n);checks.push(n)};function act(key){const o=G.storyObjects.find(o=>o.key===key);Object.assign(p,{x:o.x,z:o.z,y:o.y});b.briarRequest('world',{key});}function talk(n,c){const o=G.storyNpcs.find(o=>o.id===n);Object.assign(p,{x:o.x,z:o.z,y:o.y});b.briarRequest('open',{npc:n});b.briarRequest('choose',{line:G.storyState.conversation.node,choice:c});b.briarRequest('close');}function tick(n){for(let i=0;i<n;i++)b.update(.016);}function clear(group){for(const e of G.enemies)if(!group||e.keepGuard===group){e.hp=0;e.dead=true;}}function walk(q,name){for(let i=0;i<600;i++){const dx=q[0]-p.x,dz=q[1]-p.z,d=Math.hypot(dx,dz);if(d<16)break;b.input.jx=dx/d;b.input.jz=dz/d;b.update(.016);}b.input.jx=b.input.jz=0;walks.push({name,to:q,at:[Math.round(p.x),Math.round(p.z),Math.round(p.y)],ok:Math.hypot(p.x-q[0],p.z-q[1])<25&&p.y>=q[2]-48});}
act('rk.arrow');ok('arrow guards block mechanism',!G.storyState.flags['rk.arrow']);talk('grant','help');clear('arrow');act('rk.arrow');act('rk.gate');ok('Grant freed and moved to breach',G.storyState.flags['rk.gate']&&G.storyNpcs[0].z===-1760);act('rk.key');ok('dungeon cannot open before defense',!G.storyState.flags['rk.cells']);act('rk.breach');tick(200);ok('first attack wave arrives',G.keep.waves===1);Object.assign(p,{x:0,z:450});tick(270);ok('retreat resets only defense',G.keep.elapsed===0&&G.keep.waves===0&&G.storyState.flags['rk.gate']);Object.assign(p,{x:0,z:-1900});tick(200);const before=G.keep.elapsed;talk('grant','below'); // closed at end; test open separately
Object.assign(p,{x:-180,z:-1760,y:0});b.briarRequest('open',{npc:'grant'});tick(200);ok('dialogue freezes defense',G.keep.elapsed===before);b.briarRequest('close');Object.assign(p,{x:0,z:-1900});tick(1900);ok('thirty seconds requires clearing surviving attackers',G.keep.elapsed===30&&!G.storyState.flags['rk.breach.done']);ok('exactly seven attackers in successful attempt',G.enemies.filter(e=>e.keepGuard==='breach'&&!e.dead).length===7);clear();tick(1);ok('defense completes',G.storyState.flags['rk.breach.done']);act('rk.wheel');ok('upper route opens dungeon exit',G.storyState.flags['rk.cells']&&G.portal);
talk('felix','help');G.keep.clock=3;act('rk.timing');ok('wrong timing stays closed',!G.storyState.flags['rk.workshop']);G.keep.clock=1;act('rk.timing');act('rk.tools');talk('felix','return');ok('tools return grants one rare dagger and opens shortcut',G.storyState.flags['rk.felix.returned']&&G.storyClaims['rk.felix.lockpick']);act('rk.trial');tick(100);ok('chamber summons four guards after warning',G.enemies.filter(e=>e.keepGuard==='trial').length===4);ok('RK02 not available before combat',!b.worldRiftShards().some(s=>s.id==='RK-02'));clear();tick(1);ok('RK02 earned by chamber combat',b.worldRiftShards().some(s=>s.id==='RK-02'));for(const s of b.worldRiftShards()){Object.assign(p,{x:s.x,z:s.z,y:s.y});b.takeWorldRiftShard(s.id);}ok('both shards pending until next half',G.pendingRiftShards.found.includes('RK-01')&&G.pendingRiftShards.found.includes('RK-02')&&!b.meta.riftShards.includes('RK-01'));
for(const [name,path]of Object.entries(G.keep.walks)){const q=path[0];Object.assign(p,{x:q[0],z:q[1],y:q[2],vy:0});for(const q of path)walk(q,name);}
return {checks,waypoints:walks.length,failed:walks.filter(x=>!x.ok),counts:__world3d().counts};});if(errors.length||result.failed.length)throw Error(JSON.stringify({errors,failed:result.failed}));return {...result,errors};
}
