# Local Netlify link state (machine-specific; not needed for Git deploys)
.netlify/

# Build / preview artifacts (root-level temp files like _bladefall_deploy.zip, _preview.png, _uploader.js)
/_*

# Node
node_modules/

# OS / editor junk
.DS_Store
Thumbs.db
*.tmp
_poc_gen/
