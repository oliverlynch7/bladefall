async page=>{
 const checks=[],errors=[],ok=(n,v)=>{if(!v)throw Error(n);checks.push(n)};page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:4331/3d/?mute=1');await page.waitForFunction(()=>window.__BF3&&window.HERO3D?.ready);await page.evaluate(async()=>{const b=__BF3;await b.briarReady;b.loadMode('rl');b.meta.run=null;b.meta.bank=null;b.meta.classId='warrior';b.meta.classUnlocked={warrior:true};b.meta.introSeen=true;b.meta.tutOff=true;b.openHub();});
 for(const cid of ['berserker','ninja','reaper','chronomancer','pirate','paladin','necromancer']){
  const result=await page.evaluate(cid=>{const b=__BF3;b.openHub();b.meta.classId='warrior';b.meta.hero=b.snapOf(b.G.p);const original=JSON.stringify(b.meta.hero.weapon);b.startTrial(cid,Math.max(0,b.ZONES.findIndex(z=>z.side?.trial===cid)));const g=b.G,n=g.storyNpcs[0];for(const e of g.enemies)e.stunT=999;Object.assign(g.p,{x:n.x,z:n.z+65,y:n.y});b.briarRequest('open',{npc:n.id});const intro=BFHubDialogue.active?.lineId===`trial.${cid}.intro`;b.briarRequest('choose',{line:`trial.${cid}.intro`,choice:'lesson'});const lesson=BFHubDialogue.active?.lineId===`trial.${cid}.lesson`;b.briarRequest('close');
   g.trialKills=b.TRIALS[cid].kills-1;b.questKill({type:'grunt'});const locked=!g.portal&&g.storyState.flags['trial.proved'];Object.assign(g.p,{x:n.x,z:n.z+70,y:n.y});b.briarRequest('open',{npc:n.id});const proof=BFHubDialogue.active?.lineId===`trial.${cid}.proved`;b.briarRequest('choose',{line:`trial.${cid}.proved`,choice:'learn'});const opened=!!g.portal&&!b.meta.classUnlocked[cid];b.briarRequest('close');const hud=document.getElementById('questbox').textContent;b.nextArea();return {intro,lesson,locked,proof,opened,hud,unlocked:!!b.meta.classUnlocked[cid],equipment:JSON.stringify(b.meta.hero.weapon)===original};},cid);
  ok(cid+' mentor, teaching gate, class reward and equipment',result.intro&&result.lesson&&result.locked&&result.proof&&result.opened&&result.unlocked&&result.equipment&&result.hud.includes('Take the portal'));
 }
 await page.evaluate(()=>__BF3.openHub());await page.waitForTimeout(1000);if(errors.length)throw Error(JSON.stringify(errors));return {checks,errors,note:'Controlled trial completion; combat difficulty unchanged and not playtested by this test.'};
}